# frozen_string_literal: true

RSpec.describe Loggerx do
  it "has a version number" do
    expect(Loggerx::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
